(()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const priority={activity:100,events:96,openings:90,presence:80,security:70,sentinel:68,energy:60,climate:55,vehicle:50,network:35,voice:30,memory:25};const storeKey='jarvis_v41_preview';function load(){try{return JSON.parse(localStorage.getItem(storeKey)||'{}')||{}}catch(e){return{}}}function orbitState(){const x=load();return{activity:true,events:true,...(x.orbit||{})}}function enabled(key){return orbitState()[key]!==false}function arrange(){const cards=$$('.orbit-card');cards.forEach(el=>{const k=el.dataset.orbit;const contextual=k==='activity'||k==='events';const active=k==='activity'?el.classList.contains('active'):k==='events'?el.classList.contains('event-active'):true;const visible=enabled(k)&&(!contextual||active);el.classList.toggle('off',!visible);if(!visible)el.removeAttribute('data-slot')});const visible=cards.filter(el=>!el.classList.contains('off')&&getComputedStyle(el).display!=='none');visible.sort((a,b)=>(priority[b.dataset.orbit]||0)-(priority[a.dataset.orbit]||0));visible.forEach((el,i)=>el.dataset.slot=String(i+1))}window.jarvisArrangeOrbit=arrange;const select=$('#orbitSelect');if(select){select.addEventListener('click',e=>{const b=e.target.closest('.orbit-toggle');if(!b)return;const key=b.dataset.orbitKey;if(!key)return;setTimeout(()=>{const x=load();x.orbit={activity:true,events:true,...(x.orbit||{})};x.orbit[key]=b.classList.contains('on');localStorage.setItem(storeKey,JSON.stringify(x));arrange()},0)})}const observer=new MutationObserver(arrange);$$('.context-card,.event-card').forEach(el=>observer.observe(el,{attributes:true,attributeFilter:['class']}));addEventListener('resize',()=>requestAnimationFrame(arrange));addEventListener('orientationchange',()=>setTimeout(arrange,140));window.jarvisApplySaved=arrange;arrange()})();