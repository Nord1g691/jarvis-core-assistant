/** JARVIS V9 -> V5 compatibility bridge. Credentials never cross the event boundary. */
(()=>{'use strict';const emit=(name,detail={})=>window.dispatchEvent(new CustomEvent(name,{detail}));const safeConnection=()=>{const s=window.JARVIS_V9_CORE?.getConnection?.()||{};return Object.freeze({status:s.status||'disconnected',url:s.url||'',error:s.error||'',ok:s.status==='connected'})};const sync=()=>emit('jarvis:v5:connection',{connection:safeConnection()});const syncFromV5=()=>{try{const s=window.JARVIS_V5_CONNECTION||window.JARVIS_V5_STATE?.connection||window.haConnection||null;if(!s)return;const url=s.url||s.haUrl||s.baseUrl||'';const ok=s.connected===true||s.ok===true||s.status==='connected'||window.connected===true;if(url&&ok&&window.JARVIS_V9_CORE?.getConnection?.().status!=='connected'){window.JARVIS_V9_CORE?.adoptConnection?.(url,s.token||s.accessToken||'');}}catch(_){}sync()};window.JARVIS_V9_V5_BRIDGE=Object.freeze({sync,syncFromV5});window.addEventListener('jarvis:v9-connection',sync);window.addEventListener('jarvis:v9-ready',sync);window.addEventListener('jarvis:v9:error',sync);window.addEventListener('jarvis:v5-ready',syncFromV5);window.addEventListener('jarvis:v5-connection-ready',syncFromV5);window.addEventListener('jarvis:v5:connected',syncFromV5);window.addEventListener('load',()=>setTimeout(syncFromV5,0));})();
