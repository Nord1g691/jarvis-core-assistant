# JARVIS Core — audit / cleanup marker

This file is a temporary marker documenting the requested full audit of the current project architecture: identify stale references, legacy loaders, duplicate bootstraps, blocked/dead code paths, and ensure the active Home Assistant/HACS UI loads the intended current assets. No functional code is changed by this marker.
